<%= form_with(model: dispute, url: order_disputes_path, local: true) do |form| %>
  <% if dispute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dispute.errors.count, "error") %> prohibited this dispute from being saved:</h2>

      <ul>
      <% dispute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id %>
  </div>

  <div class="field">
    <%= form.label :replies_id %>
    <%= form.text_field :replies_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.number_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
