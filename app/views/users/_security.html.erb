<div class="tab-pane fade <%= "show active" if params[:collapse] == "security" %>" id="security" role="tabpanel" aria-labelledby="security-tab">
  <%= form_with model: @user, url: user_registration_path, html: { id: "change-pass-form",method: :put } do |f| %>
    <div class="form-group row">
      <label class="col-sm-2 col-form-label tx-right">Contraseña Actual</label>
      <div class="col-sm-10">
        <%= f.password_field :current_password, autocomplete: "off", class: 'form-control', required: "true" %>
      </div>
    </div>
    <div class="form-group row">
      <label class="col-sm-2 col-form-label tx-right">Nueva Contraseña</label>
      <div class="col-sm-10">
        <%= f.password_field :password, autocomplete: "off", class: 'form-control', required: "true"  %>
      </div>
    </div>
    <div class="form-group row">
      <label class="col-sm-2 col-form-label tx-right">Confirmar nueva Contraseña</label>
      <div class="col-sm-10">
        <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control', required: "true" %>
      </div>
    </div>

    <div class="form-group row mg-b-0">
      <div class="col-sm-10">
        <%= f.submit "Cambiar contraseña", class: 'btn btn-p-purple-100' %>
      </div>
    </div>
  <% end %>
</div>
