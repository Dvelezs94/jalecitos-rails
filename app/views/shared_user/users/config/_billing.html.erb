<div class="card">
  <div class="card-header" id="headingFour">
    <h5 class="mb-0">
      <button class="btn btn-link collapsed" data-toggle="collapse" data-target="#billing" aria-expanded="<%= (params[:collapse] == "billing")? "true" : "false" %>" aria-controls="billing">
        Facturación
        <i class="fa fa-angle-right" aria-hidden="true"></i>
        <i class="fa fa-angle-down" aria-hidden="true"></i>
      </button>
    </h5>
  </div>
  <div id="billing" class="collapse <%= "show" if params[:collapse] == "billing" %>" aria-labelledby="headingFour" data-parent="#accordion">
    <div class="card-body">
      <div class="Facturacion-content">
        <%= render partial: 'users/billing_profile', collection: @billing_profiles %>
        <div style="text-align: right;">
          <button id="billingprofilebtn2" class="default-btn">Nuevo Perfil Tributario</button>
        </div>
      </div>
    </div>
  </div>
</div>
