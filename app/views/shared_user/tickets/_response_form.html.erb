<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <div class="transacciones-tab-text">
        <div class="ticket-text">
        <%= form_with(model: TicketResponse.new) do |form| %>
        <%= form.text_area :message, class: "form-control" %>
        <%= form.hidden_field :ticket_id, :value => params[:id]  %>
        <div class="submit-right">
          <%= form.submit "Continuar", class: "btn continue-button" %>
        </div>
        <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
