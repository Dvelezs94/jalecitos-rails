$("[paginated-notif] > nav").remove(); //this deletes pagination
$("[paginated-notif]").append('<%= j render(@notifications) %>');
stylize_notifications();
<% if @notifications.next_page %>
  $("[paginated-notif]").append('<%= j paginate(@notifications) %>');
<% end %>

<% sleep 1 %>
