<div class="row_new">
  <% if @notifications.count > 0 %>
    <div class="row">
      <div class="paginated-elements notifarea">
        <%= render @notifications %>
      </div>
    </div>
    <div class="pagination-links">
      <%= paginate @notifications %>
    </div>
  <% else %>
  <div class="container" style="text-align: center;">
    No hay nada para mostrar
  </div>
  <% end %>
</div>
