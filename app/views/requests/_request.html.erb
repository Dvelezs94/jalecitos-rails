<a href="<%= request_path(request) %>">
<div class="single-request">
  <div class="req_info">
    <p class="req_location"><%= truncate(request.location, length: 25)%></p>
    <p class="req_title"><%= truncate(request.title, length: 50) %></p>
  </div>
  <div class="req_budget">
    <p class="COAH3"><%= request.budget %> MXN</p>
  </div>
</div>
</a>
