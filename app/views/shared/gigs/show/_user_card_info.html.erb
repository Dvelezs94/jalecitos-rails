<div class="mentos-areapag3">
  <div class="mentos-authors">
    <%= link_to image_tag(avatar_display_helper(@gig.user.image_url), class: "nav-avatar"), user_path(@gig.user.slug) %>
    <div class="mentos-title2">
      <h1><%=link_to @gig.user.alias, user_path(@gig.user.slug), style: "color: black;" %>
        <%= "<span> #{icon("fas", "check-square", title: "Verificado")} </span>".html_safe if @gig.user.verified %>
      </h1>
      <p class="hace2"><%= @gig.user.location %></p>
      <div class="mentosstar-list">
        <%= star_display_helper(@gig.user.score.employee_score_average) %>
        <p class="hace1">Activo(a) hace <%= distance_of_time_in_words_to_now(@gig.user.current_sign_in_at) %></p>
      </div>
    </div>
  </div>
  <div class="mentosdates">
    <p class="datesin">Miembro desde <%= distance_of_time_in_words_to_now(@gig.user.created_at) %></p>
    <p class="edadd1">Edad: <%= (@gig.user.age.present?)? @gig.user.age : "Indefinido" %></p>
    <div class="mentoslong-content">
      <pre class="elit55"><%= (@gig.user.bio.present?)? @gig.user.safe_bio : "Sin descripción." %></pre>
    </div>
  </div>
</div>
