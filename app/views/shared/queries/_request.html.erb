<div class="mg-t-10 req-card" loaded_in_map="false" lat="<%= request.lat %>" lng="<%= request.lng %>" title="<%= request.title %>">
  <div class="card">
    <div class="card-body pd-b-5-f pd-t-10-f">
      <small class="tx-gray-600">Publicado hace <%= distance_of_time_in_words_to_now(request.created_at) %></small>
      <p class="card-title tx-16"><%= request.title %></p>
      <div class="d-flex justify-content-between align-items-center">
        <small><%= request.offers_count %> ofertas</small>
        <small><strong><%= request.budget %></strong></small>
      </div>
    </div>
    <div class="card-footer d-flex justify-content-between">
      <%= link_to request_path(request.slug), class: "share_item_url" do %>
        Compartir
      <% end %>
      <%= link_to request_path(request.slug) do %>
        Ver más <i data-feather="chevron-right" class="feather-12 d-none d-md-inline"></i>
      <% end %>
    </div>
  </div>
</div>
