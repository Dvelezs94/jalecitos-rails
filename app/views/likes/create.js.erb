item = $(".likes-<%= j @gig.slug %>");
if( item.hasClass("with_number") ) {
  item.html("<%= j render 'gigs/likes', gig: @gig, with_number: true %>")
}
else {
  item.html("<%= j render 'gigs/likes', gig: @gig, with_number: false %>")
}
