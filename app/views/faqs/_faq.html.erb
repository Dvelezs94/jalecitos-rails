<div class="card">

  <!-- Card header -->
  <a class="collapsed" data-toggle="collapse" data-parent="#faq_gig" href="#<%= "a#{faq.id}" %>" aria-expanded="false" aria-controls="<%= "a#{faq.id}" %>">
  <div class="card-header" role="tab" id="<%= "a#{faq.id-1}b" %>">
      <h5 >
        <%= faq.question %>
        <i class="fas fa-angle-down rotate-icon float-right"></i>
      </h5>
  </div>
</a>

  <div id="<%= "a#{faq.id}" %>" class="collapse" role="tabpanel" aria-labelledby="<%= "a#{faq.id-1}b" %>" data-parent="#faq_gig">
    <div class="card-body">
      <%= faq.answer %>
    </div>
  </div>

</div>
