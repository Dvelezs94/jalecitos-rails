$(".loading").hide();
<% if @success %>
  $(".current_images").append("<%= j render partial: 'galleries/image', collection: [@gig.images.last] %>");
<% else %>
  alert("<%= j @gig.errors[:images][0].html_safe %>");
<% end %>
