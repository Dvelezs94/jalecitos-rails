$(".message_view_box").prepend('<%= j render "conversations/conversation_content", messages: @messages.sort %>');
$('.pagination').remove();
<% if @messages.next_page %>
  $(".message_view_box").prepend('<%= j paginate(@messages) %>');
<% end %>
<% sleep 1 %>
