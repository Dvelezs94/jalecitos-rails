<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label class="d-inline-block">nombre de categoría
  </label>
      <%= form.text_field :name, class: 'name-class name-0 form-control required', maxlength: 100 %>
  </div>

  <div class="form-group">
    <label class="d-inline-block">meta_title
  </label>
      <%= form.text_area :seo_title, class: 'name-class name-0 form-control required', maxlength: 100 %>
  </div>
  <div class="form-group">
    <label class="d-inline-block">meta_description </label>
    <%= form.text_area :seo_description,
    class: "description-class description-0 form-control required", rows: 5 %>
  </div>


  <div class="form-group justify-content-center">
    <label for="tags" class="d-block">keywords</label>
    <%= form.text_field :seo_keywords, class: 'form-control tagsinput' %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
