if (<%= @success %>){
  $("#new_review_content").html("<span class='d-flex justify-content-center'>Reseña enviada <i data-feather='check' class='feather-18 text-success ml-1'></i></span>");
  $("#paginated_reviews").prepend("<%= j render(partial: 'shared/reviews/review', collection: [@review]) %>");
  $("#paginated_reviews").collapse("show");
}
else{
  show_error("<%= @review.errors.full_messages.first || 'Ha ocurrido un error inesperado, por favor, inténtalo más tarde' %>")
}
feather.replace();
