$("#paginated_reviews > a").remove(); //this deletes pagination
$("#paginated_reviews").append('<%= j render(partial: "shared/reviews/review", collection: @reviews) %>');
<% if @reviews.next_page %>
  $("#paginated_reviews").append('<%= j link_to_next_page(@reviews, "Cargar más", remote: true, param_name: :reviews, data: {disable_with: "Cargando..."}) %>');
<% end %>

<% sleep 1 %>
